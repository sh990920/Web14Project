<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b">
	
	<!-- 새댓글작성  -->
	<insert id="reply_insert" parameterType="vo.ReplyVO">
		insert into reply values(
			seq_reply_replyIdx.nextVal, 
			#{movieName}, 
			#{replyComment}, 
			#{userId},  
			sysdate
		)
	</insert>
	
	<!-- 수정 -->
	<update id="update">
        UPDATE reply
        SET replyComment = #{replyComment}, update_date = NOW()
        WHERE userId = #{userId} and movieName = #{movieName}
    </update>
	
		
	<!-- 삭제-->
	<delete id="delete_reply">
		delete from reply where movieName = #{movieName} and userId = #{userId}
	</delete>
	
	<!-- 댓글 불러오기-->
	<select id="reply_" resultType="vo.ReplyVO">
		select * from reply
	</select>
</mapper>






























